[general]
#磁盘可用率
#avail = 20

#需要保留日志文件的天数
intervalTime = 24

#if value equal 1, delete had to action; else, compression had to action.
Delete = 1

#time for waite single logfile to compress,do not edit it until you had very known it.
#wait_time = 60

#logfile size(Mbit) that this value will be scan.
#size = 10

#where have to save compression package.
#tar_path = /tmp

[filter]
#强匹配规则，若使用这项，将使用python的正则进行严格匹配(re.match)，仅匹配规则的文件会被程序处理
#reList = (.*\.log)\.\d{4}-\d{1,2}-\d{1,2}(\.\d{1,2})?||.*\d{4}-\d{2}-\d{1,2}\.tar\.gz||.*\d{4}-\d{2}-\d{2}(-\d{1,2})?\.log||.*

#exclude_path = "/media"


[sftp]
#Marked this as 1, open SFTP features.
#SP = 0  
#
#SftpHost = 10.127.26.241
#SftpPort = 58422
#SftpHostUser = root
#SftpHostPwd = password



[mail]
##Marked this as 1, open mail notification if scanning is start.
#SM = 0  
#
#smtpServer = your_smtp.com
#smtpUser = your_user
#smtpPwd = your_password
#fromMail = from_mail
#toMail = defage@gmail.com
