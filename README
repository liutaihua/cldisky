Installation Instructions
    python setup.py install (request setuptools,You can use[CentOS5.0 platform]:yum install python-setuptools.noarch)

How to?
    1. cldisky (requirement)
    2. vim /etc/cldisky.conf,and configuration 
    3. cldisky start
    4. cldisky stop
